The photo was saved and attached to the draft as
`<%= ExBlogWeb.Prompt.escape_text(@cover, 512) %>`.

When you confirm the draft, the validated image and Markdown article will be
committed to Git together.

The workflow did not advance. Now <%= ExBlogWeb.Prompt.escape_text(@next_step, 160) %>.
