Selected title: <%= ExBlogWeb.Prompt.escape_text(@title, 160) %><%= if Map.get(assigns, :title_generated?, false) do %> (generated with OpenRouter)<% end %>.

Reply `generate SEO` to produce the SEO title, description, alternative text,
and tags with OpenRouter alongside the article. Reply `skip` to create the
draft without generated SEO metadata.
