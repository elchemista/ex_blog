I could not generate that revision. The current preview is still intact and
nothing was written to Git. Try a different `modify: ...` instruction, reply
`confirm` to save the current version, or `cancel`.
