The draft “<%= ExBlogWeb.Prompt.escape_text(@title, 160) %>” is saved in Git and indexed, but it is not public yet.

Publish it now at <%= ExBlogWeb.Prompt.escape_text(@public_url, 512) %>?
Reply “yes” to publish it, or “no” (or “cancel”) to keep it as a draft.
