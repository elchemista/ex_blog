Send between one and <%= ExBlogWeb.Prompt.escape_text(@maximum || 3, 8) %> valid public HTTP(S) source URLs in one message. URLs with credentials, whitespace, or unsupported schemes are not accepted. The workflow is still waiting for sources.
